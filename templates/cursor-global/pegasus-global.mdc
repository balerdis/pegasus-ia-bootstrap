---
description: Legacy Cursor fallback for locating workspace-local Pegasus guidance
alwaysApply: true
---

# Legacy Pegasus Cursor Fallback

Version: {{GLOBAL_TEMPLATE_VERSION}}

Cursor support is secondary. Prefer `.github/copilot-instructions.md`, `.github/agents/pegasus-orchestrator.agent.md`, `.github/instructions/`, `AGENTS.md`, and the exact manual references those local assets select.

This global rule does not authorize phase execution, edits, or persistence. If workspace-local Pegasus entry points or required references are absent, stop and ask which workflow applies; never reconstruct their contracts. Until local authority is loaded, keep work local and reversible and do not create product or infrastructure scaffolding.
