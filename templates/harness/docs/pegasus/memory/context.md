# Memory: Context

Read this file at the start of every session and after context compaction.

## Project Facts

- Project: `{{PROJECT_NAME}}`
- Target path: `{{TARGET_PATH}}`
- Created: `{{DATE}}`

## Current Context

- Primary IDE guidance: VS Code/Copilot assets under `.github/`.
- Portable guidance: `AGENTS.md` and `docs/pegasus/`.
- Legacy compatibility: `.cursor/rules/`.
- Project-selected Copilot model: TBD. Record the preferred model here when the team chooses one; Pegasus docs do not provide hard per-phase runtime routing.

## Write Rules

Update this file when stable project facts, constraints, environments, IDE guidance, model preference, or assumptions change. Merge new facts into the existing useful history instead of replacing prior context.
