---
description: Legacy Cursor pointer to workspace-local Pegasus Memory ownership
alwaysApply: true
---

# Pegasus Memory Compatibility

This Cursor rule is a compatibility pointer only. Prefer `.github/instructions/pegasus-memory.instructions.md`; specialists load `.github/references/shared/persistence.md` and their exact phase contract.

Active artifact references use `docs/pegasus/changes/<change-id>/`; root phase files are templates.

Do not reconstruct recovery, persistence, status, or result procedures here. Never use `docs/pegasus/memory/` as a backend or fallback. If an exact local owner is missing, block before persistence.
